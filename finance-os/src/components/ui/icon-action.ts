// Shared styling for the compact icon-only actions in card footers and list
// rows (view / import / edit / delete). Kept in one place so the account
// list, transaction list and account cards stay visually identical.
export const iconActionClasses =
  "inline-flex items-center justify-center size-9 rounded-md text-muted hover:text-purple hover:bg-border transition-colors duration-200"
